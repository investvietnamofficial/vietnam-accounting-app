# Production environment for Next.js web app
# These are embedded into the Next.js client bundle at build time.

# Backend API base URL (internal Docker network: http://backend:8000)
NEXT_PUBLIC_API_URL=http://backend:8000

# Set to 'production' to disable dev-only features (docs, debug UI, etc.)
NEXT_PUBLIC_APP_ENV=production
